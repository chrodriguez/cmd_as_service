Request to run command
======================

A request to run command:

<%= cmd %>

-------------------------------------------------------------------------------
To confirm command execution, you must access the following URL:

<%= url(token_url) %>
